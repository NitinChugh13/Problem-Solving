#include<stdio.h>
#include<math.h>
int main () {
    int a,b,c ;
    printf("enter a number a and b");
    scanf("%d%d",&a,&b);
    c=pow(a,b);
    printf("the a power b is %d",c);
    return 0;
    
}